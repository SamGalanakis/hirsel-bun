/**
 * Toaster component - container for toast notifications
 * Uses Basecoat toast system
 */
import { type Component } from 'solid-js';

export const Toaster: Component = () => {
  return <div id="toaster" class="toaster" />;
};
