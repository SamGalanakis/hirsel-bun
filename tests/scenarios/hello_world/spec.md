# Hello World

## Goal
Create a simple Python script that prints "Hello, World!" to stdout.

## Requirements

1. Create a file `hello.py` that prints exactly: `Hello, World!`
2. The script should be executable with `python hello.py`

## Acceptance Criteria

- Running `python hello.py` outputs exactly `Hello, World!` (with newline)
